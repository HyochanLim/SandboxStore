<header>
    <div><a href="/">WDE</a></div>
    <nav>
        <ul>
            <% if (typeof isAuth !== 'undefined' && isAuth) { %>
            <li>
                <button>Logout</button>
            </li>
            <% } %>
        </ul>
    </nav>
</header>